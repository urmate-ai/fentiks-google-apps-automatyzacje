# Certyfikaty TLS dla Netlify Functions

Umieść plik `l.kmiecik.adm.pem` w tym katalogu przed wdrożeniem na Netlify. Plik powinien zawierać zarówno klucz prywatny, jak i certyfikat wymagany przez Multiinfo Plus.

> ⚠️ Plik PEM nie powinien być dodawany do repozytorium Git.
